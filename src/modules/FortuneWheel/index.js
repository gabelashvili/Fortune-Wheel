import * as constants from 'modules/FortuneWheel/constants'
import reducers from 'modules/FortuneWheel/reducers'
import * as actions from 'modules/FortuneWheel/actions'
import * as selectors from 'modules/FortuneWheel/selectors'

const moduleName = 'GetFortuneWheelSettings'

export {
  constants,
  reducers,
  moduleName,
  selectors,
  actions,
}
